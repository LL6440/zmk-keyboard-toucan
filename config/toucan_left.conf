../boards/shields/toucan/toucan.conf
# Debug logging — connecte-toi en Serial dans PuTTY (115200 baud) pour voir
# les traces de circular_scroll.c en temps réel.
# Décommenter pour activer, recommenter pour désactiver.
CONFIG_ZMK_LOG_LEVEL_DBG=y
CONFIG_LOG=y
